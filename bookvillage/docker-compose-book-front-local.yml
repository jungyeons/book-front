services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: book-village-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_LOCAL_PORT:-80}:80"
    networks:
      - book-village-net

networks:
  book-village-net:
    external: true
    name: book-village-net
